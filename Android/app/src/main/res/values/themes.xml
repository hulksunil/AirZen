<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AirZen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AirZen" parent="Base.Theme.AirZen" />

    <style name="labelTheme">
        <item name="android:textSize">15sp</item>
    </style>

    <style name="MainActivityBoxes">
        <!-- color related stuff for the tiles is in the layout because it needed to be rounded
        corners so we just changed the colors its using instead of the entire theme -->
<!--        <item name="android:background">@color/timberWolf</item>-->
    </style>
</resources>